{% extends 'layouts/base.html.twig' %}


{% block title 'Register!' %}

{% block body %}
	{% for flashError in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}

	<h1 class="text-center mb-5">Create account</h1>
	<div class="row">
		<div class="col-md-4 mx-auto">
			{{ form_start(registrationForm,{'attr':{'novalidate':'novalidate'}}
			) }}
			<div class="d-flex justify-content-between">
				{{ form_row(registrationForm.firstName) }}
				{{ form_row(registrationForm.lastName) }}
			</div>
			{{ form_row(registrationForm.email) }}
			{{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
			{{ form_row(registrationForm.agreeTerms) }}
			<button type="submit" class="btn btn-dark w-100" formnovalidate>Register</button>
			<p class="mt-5 mb-3 text-center text-muted">Already have an account ?<a href="{{path('app_login')}}">Log in</a>
			</p>
			{{ form_end(registrationForm) }}
		</div>
	</div>

{% endblock %}

