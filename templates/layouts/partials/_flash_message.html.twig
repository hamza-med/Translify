{% for type, messages in app.flashes(['success','error','info'] )%}
	{% for message in messages %}
		<div class=" text-center h5 alert alert-{{type}}" role="alert">{{message}}</div>
	{% endfor %}
{% endfor %}
